environment:
  matrix:
    - nodejs_version: "4"
    - nodejs_version: "6"

platform:
  - x64
  - x86

install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - npm install zmq-prebuilt

test_script:
  - node node_modules/zmq-prebuilt/examples/smoketest.js

build: off
